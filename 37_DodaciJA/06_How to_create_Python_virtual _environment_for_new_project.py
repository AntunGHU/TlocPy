# 1'54

# Interesantan pristup:
# Kreira nadmapu projekta koja ce imati podmapu "env" i podmapu project (u koju ce zapravo ici kod projekta)
# Kreira mapu venv-a nakon sto je s cd usao u nadmapu project-a
# ? python -m venv env
# otvara nadmapu projekta sa vsc-kodom
# Kreira se submapu projekta (cod-mapu) pokraj mape venv-a sa vscodom
# Kreira se npr hello.py fajl unutar cod-mape (sa vsc)  sto ce natjerati aktivaciju py-ekstenzije i automatski potraziti sve venv-e unutar tog dir-a
# klika na python-verziju status bara: u ponudi je i nasa mapa venv-a
# Klikamo na tu opciju i ispred prompta ali se ne pojavljuje (venv) znak aktivacije venv-a!?!?
# KAd sam se vec razocarao kliknem na novi term i zaista, (env) se pojavio  ispred prompt-linea!!!
# ? source /home/antun/Documents/EnvProjVjez/env/bin/activate
# ? antun@ub:~/Documents/EnvProjVjez$ source /home/antun/Documents/EnvProjVjez/env/bin/activate
# ? (env) antun@ub:~/Documents/EnvProjVjez$
