# 14'14

# Why OOP: omogucava logiku i hijerarhiju slicnu onoj koju univerzalno primjenjujemo
    # poker: primjer bez OOP-samo funkcije: def deal_hand(deck, num_cards), def reset_deck(), def display_card(card), def display_hand(hand)
    # poker: sa OOP: deck=Deck(), deck.shuffle(), hand1=deck.deal_hand()
# Encapsualtion: grouping of public & private attributes & methods into programatic clas, making abstraction possible
# Abstarction: exposing only "relevant" data in class interface hiding private attributes & methods (aka inner workings) from user