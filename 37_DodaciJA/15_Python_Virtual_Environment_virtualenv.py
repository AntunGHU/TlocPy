# 8'52

# Python Virtual Environment (virtualenv) | Python Best Practices

# video je novi, 2021, i donosi novost a to je da je kao virtualenv sada builtin u Py i primjenjuje se sa komandom potpuno slicnom venv-modulu

# ? python3 -m vitrualenv venv
# ? activate
# ? pip install ...
# ? deactivate
